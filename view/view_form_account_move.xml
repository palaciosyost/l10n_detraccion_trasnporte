<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="form_account_move_inherit_detraccion" model="ir.ui.view">
        <field name="name">account.move.view.form.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='invoice_tab']" position="after">
                <page name="detraccion_tab" string="Detracción">
                    <group>
                        <field name="direccion_origen"/>
                        <field name="direccion_destino"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

</odoo>
